# ============================
# Backend Environment (Example)
# ============================
# Copy this file to ".env" in the backend/ directory for local development:
#   cp backend/.env.example backend/.env
#
# In Vercel/v0 preview, .env files are not used. Set these in Project Settings > Environment Variables.

# PostgreSQL connection string (choose one of the examples below)
# Local Postgres (Docker or local install)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qsatlms?schema=public"
# Neon (replace with your values)
# DATABASE_URL="postgresql://USER:PASSWORD@ep-sunny-123456.us-east-1.aws.neon.tech/qsatlms?sslmode=require&pgbouncer=true&connect_timeout=15"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qsatlms?schema=public"

# App
NODE_ENV="development"
PORT="4000"

# Security / Auth
# Use a long, random string. Keep this secret.
JWT_SECRET="replace-with-a-strong-random-secret"
# Token lifetime format: e.g., 15m, 1h, 7d
JWT_EXPIRES_IN="7d"
# Bcrypt salt rounds used for hashing passwords
BCRYPT_SALT_ROUNDS="12"

# CORS
# Comma-separated list of allowed origins for the API
CORS_ORIGIN="http://localhost:3000,http://localhost:5173"

# Logging
# Set to "true" to enable Prisma query logging via DEBUG
# (you can also configure logging in the Prisma client initialization)
DEBUG_PRISMA="false"

# Optional email redirect for auth flows (if applicable in your app)
# FRONTEND_URL is used for email verification links/password resets etc.
FRONTEND_URL="http://localhost:3000"
